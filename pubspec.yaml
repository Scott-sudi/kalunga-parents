name: kalunga_parents
description: Application mobile officielle des parents — Institut Kalunga.
publish_to: "none"
version: 1.2.10+15

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.3.3
  flutter_riverpod: ^2.6.1
  intl: ^0.20.2
  equatable: ^2.0.7
  image_picker: ^1.2.3
  flutter_local_notifications: ^22.3.0
  url_launcher: ^6.3.2
  firebase_core: ^4.13.0
  firebase_messaging: ^16.5.0
  audioplayers: ^6.5.1
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/branding/logo.png
    - assets/sounds/kalunga_alert.wav
